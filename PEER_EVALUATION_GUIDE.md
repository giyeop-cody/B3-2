# 🤝 동료 평가(Peer Review) 종합 가이드

## 1. 개요 및 평가 환경 정의
본 가이드는 **동일한 Mini Git 과제를 수행하는 학습자 간 동료 평가(Peer Review)**를 위한 안내서입니다.
평가자는 과제 수행 여부 및 이해도 수준에 따라 다양할 수 있습니다.

* **초심자/과제 미완료 평가자**: 코드를 완벽히 이해하지 못해도 복사-붙여넣기 명령어 실행과 **화면 출력 비교(Visual Diff)**만으로 100% 명확히 평가 가능.
* **숙련자/과제 완료 평가자**: 알고리즘 정합성(Merge Sort 안정 정렬, Kahn's Algorithm, 역색인 $O(1)$) 및 핵심 Q&A 질의를 통해 심층 평가 진행.

---

## 2. 평가자 수준별 5분 쾌속 채점 흐름도

```
[평가 시작] 
    │
    ├─► 1단계: 터미널에서 `python main.py` 또는 `python run_evaluation.py` 실행
    │
    ├─► 2단계 [초심자 평가자]: 화면 출력 문구가 기대 결과 표와 일치하는지 눈으로 비교 (PASS/FAIL)
    │
    └─► 3단계 [숙련자 평가자]: 소스 코드 내 금지어(`sorted(`, `.sort(`) 정적 검사 및 4개 핵심 Q&A 질의
```

---

## 3. 평가자 수준별 맞춤형 평가 전략

### 🟢 유형 A: 과제를 아직 시작하지 않았거나 잘 모르는 평가자 (초심자 가이드)
* **평가 방법**: 아래 터미널 명령어 블록을 그대로 복사하여 실행한 뒤, **결과 문구가 똑같이 나오는지**만 확인합니다.
* **핵심 체크 포인트 (3가지)**:
  1. `LOG` 입력 시 `Initial commit`이 최상단(부모)에 먼저 나오는가?
  2. `SEARCH "login"` 입력 시 `Add login feature` 커밋만 추출되는가?
  3. `SORT-COMPARE` 입력 시 Merge Sort가 Bubble Sort보다 빠른 밀리초(ms) 수치가 찍히는가?

---

### 🔵 유형 B: 과제를 완성했거나 잘 아는 평가자 (숙련자 가이드)
* **평가 방법**: 기능 검증 외에 **소스 코드 정적 분석** 및 **핵심 질문 답변**을 검증합니다.
* **핵심 코드 검증 포인트 (3가지)**:
  1. **내장 정렬 금지 준수**: `grep -r "sorted(" .` 및 `grep -r "\.sort(" .` 검색 시 내장 정렬 사용이 없어야 함 (`mini_git/engine/sorter.py`의 `merge_sort`만 사용).
  2. **Merge Sort 안정성**: `sorter.py`에서 `left[i] <= right[j]` 로 부등호가 처리되어 앞쪽 요소의 입력 순서가 보존되는지 확인.
  3. **위상 정렬 indegree**: `graph.py`에서 Kahn's Algorithm 기반 `indegree` 차감 루프 확인.

---

## 4. 초간단 복사-붙여넣기 통합 테스트 스크립트

평가자는 터미널에 아래 한 줄 명령어를 실행하여 전체 검증 결과가 담긴 마크다운 파일(`evidence/evaluation_evidence.md`)을 자동 생성시킬 수 있습니다.

```bash
python run_evaluation.py
```

* 실행 후 `evidence/evaluation_evidence.md` 파일을 열어 모든 명령어가 정상 실행되었는지 확인하면 평가가 종료됩니다.
